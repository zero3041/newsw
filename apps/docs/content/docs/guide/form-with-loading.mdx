---
title: Form with Loading
description: Learn how to build a form with a loading overlay, handling form state and UX during async operations.
---

## Usage

Simply wrap your form fields with the `LoadingOverlay` component and set `isLoading = true` when submitting data.

If you're using a mutation from React Query, pass the `isPending` state to `isLoading`.

<ComponentPreview name="RecFormWithLoading" className="max-w-[350px] mx-auto" />
