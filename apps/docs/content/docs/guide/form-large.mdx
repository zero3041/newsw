---
title: Large Form
description: Build a multi-section form with arrays, selectors and toggles.
---

## Usage

For large forms, you can split the form into multiple sub-sections. Within each section, use `useFormContext` to access the parent form.

<ComponentPreview name="RecFormLargeForm" className="max-w-[700px] mx-auto" />
